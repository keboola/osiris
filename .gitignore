# Testing environment - ignore artifacts but keep directory accessible to Claude
testing_env/**/**
testing_env/.gitignore
testing_env/.env.example

# ChatGPT mempack
mempack.txt
mempack-*.txt

# Backup files
*.bak

# Global artifacts that may appear anywhere
.osiris_sessions/
.osiris_prompts/
output/
.aiop-annex/*

# Test artifacts that shouldn't be in repo root
*MagicMock*
*mock.*

# Compilation and execution artifacts (should only be in testing_env/tmp/)
compiled/
_artifacts/
test_*/
*_test/
final_test/
tools/mempack/gen/*

# keep tests in repo, ignore only caches and reports
.pytest_cache/
coverage.xml
htmlcov/

# Internal tools
.claude/
.audit/
.playwright-mcp/

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
.pytest_cache/

# Python packaging
# NOTE: Contract v1 uses build/ for compiled artifacts (see below)
# Python packaging build dir conflicts; use dist/ for packaging
dist/
*.egg-info/
*.egg
uv.lock

# Code quality tool caches
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# IDE/Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
.tmp/

# Environment variables
.env
.env.local
.env.*.local

# Old Logs
logs/

# Filesystem Contract v1 (ADR-0028) - new directories
run_logs/            # Per-run execution logs (ephemeral)
aiop/**/annex/       # AIOP annex shards (optional to commit)
.osiris/cache/       # Discovery/profiling cache
.osiris/index/counters.sqlite  # Incremental run ID counters
.osiris/index/counters.sqlite-wal
.osiris/index/counters.sqlite-shm

# build/ - Deterministic compiled artifacts (versionable, team decides to track or ignore)
# Recommended: Track in Git for reproducibility, or add to .gitignore if too large
# build/pipelines/  # Uncomment to ignore compiled manifests

# Legacy logs/ directory removed per ADR-0028

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml
