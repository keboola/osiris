# Intentionally invalid spec to test validation
name: Invalid.Component  # Invalid: uppercase not allowed
version: 1.0  # Invalid: incomplete semver
modes: []  # Invalid: empty array
capabilities:
  unknownCapability: true  # Invalid: not in schema
configSchema:
  # Missing required 'type' field
  properties:
    field: {type: string}
secrets:
  - connection/password  # Invalid: missing leading /
  - /valid/pointer
redaction:
  strategy: encrypt  # Invalid: not in enum [mask, drop, hash]
