# Core dependencies
rich>=13.0.0            # Rich terminal formatting for CLI
pyyaml>=6.0.2           # YAML parsing (security fix for CVE-2020-14343)
duckdb>=0.9.0           # Local SQL engine and per-run data exchange
pydantic>=2.7.0         # Plan / Step / Pins / Policy models
httpx>=0.27.0           # HTTP client for the cf-ng REST API
typer>=0.12.0           # CLI framework (serve / freeze / run / doctor)
python-dotenv>=1.0.0    # Environment variable loading from .env files

# MCP Server dependencies
mcp>=1.2.1              # Model Context Protocol Python SDK

# Note: For development dependencies, use: pip install -e ".[dev]"
# This installs the package with all development tools defined in pyproject.toml
